# Test environment variables - SAFE FAKE CREDENTIALS ONLY
DISCORD_TOKEN=test_token_123456789
DISCORD_CLIENT_ID=123456789
DISCORD_PUBLIC_KEY=test_public_key

# Discord Backup Channel (where backup threads will be created)
BACKUP_CHANNEL_ID=987654321

BOT_REPORT_CHANNEL_ID=
BOT_OWNER_IDS=

MONGO_HOST=test.mongodb.net
MONGO_USERNAME=testuser
MONGO_PASSWORD=testpass
MONGO_DB=testdb
MONGO_API_KEY=test_api_key

# AWS S3 Configuration for Backups
AWS_ACCESS_KEY_ID=test_access_key
AWS_SECRET_ACCESS_KEY=test_secret_key
AWS_REGION=us-east-1
S3_BACKUP_BUCKET=test-bucket

# Backup Configuration
ENABLE_NIGHTLY_BACKUP=true

# Backup schedule (24-hour format)
BACKUP_HOUR=2
BACKUP_MINUTE=0

# Compression method: 'gzip' (recommended) or 'brotli' (better compression, slightly slower)
COMPRESSION_METHOD=gzip

# Linode Server SSH Configuration (for remote PM2 commands)
LINODE_SSH_USER=root
PM2_DEXCOM_APP_NAME=testapp

